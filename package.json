{
  "name": "dhaka-bus-service",
  "version": "1.0.0",
  "description": "Smart Bus Portal with Next.js frontend and NestJS backend",
  "scripts": {
    "build": "cd frontend && npm run build",
    "dev": "cd frontend && npm run dev",
    "start": "cd frontend && npm run start"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "dependencies": {
    "@nestjs/common": "^11.1.6",
    "@nestjs/core": "^11.1.6",
    "@nestjs/platform-express": "^11.1.6",
    "pusher": "^5.2.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
